proxy_type: auto
# Set this so we get the libraries, but don't set the arduino15 path, so that
# we get the user's board definitions
sketchbook_path: ./build/Arduino
directories:
  user: ./build/Arduino
board_manager:
  additional_urls:
  - https://candykingdom.github.io/firefly-v2-board/package_candykingdom_index.json

